pipeline{
    agent any

    stages{
       stage('prep'){
            steps{
		echo "prep"
            }
        }
       stage('build'){
            steps{
		echo "build"
            }
        }
       stage('deploy'){
            steps{
		echo "deploy"
            }
        }
    }
}
